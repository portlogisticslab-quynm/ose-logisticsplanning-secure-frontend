"use strict";
// Use an empty string when frontend and backend are served from the same origin.
// For a separately hosted frontend, set this to the backend URL, e.g.
// window.APP_CONFIG.API_BASE_URL = "https://your-backend.onrender.com";
window.APP_CONFIG = {
  API_BASE_URL: ""
};
